<div class="container mb-3 pe-5 ps-5 mt-5">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-3">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>
