{% extends 'base.html.twig' %}

{% block title %}Modifier la spécialité {% endblock %}

{% block body %}
<style>
    .modifierSpeciqlite { background-image: url("https://www.the20.com/wp-content/uploads/2018/12/computer-at-night.jpg");background-repeat: no-repeat;background-size: cover;height: auto}
</style>

<div class="">
    <div class="modifierSpeciqlite pb-5 pt-3">
        <div class="row text-center">
            <div class="col-md-12">
                <h3 class="h1 text-white">
                    {{ specialite.CodeSpecialite ~' ('~ specialite.libelleSpecialite ~' )' }}
                </h3>
            </div>
            <div class="col-md-12 text-light">
                Welcome to <br>
                <span class="text-primary h1">PHIBMAT-fees</span>
            </div>
        </div>
    </div>

    <div class="row bg-white">
        <div class="col-md-4 col-sm-6 text-center mt-5 pt-5">
            <h3>Modifier la spécialité  {{ specialite.CodeSpecialite }}</h3>
            {# <div class="text-center mb-2">
                🛑🛑 la modif n'aura pas lieu s'il existe déjà le étudiants inscris sous cette spéciali <b>Symfony</b>
            </div> #}
        </div>
        <div class="col-md-6 col-sm-6 bg-light mb-3 rounded-4 p-4 mt-3 pt-5">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
